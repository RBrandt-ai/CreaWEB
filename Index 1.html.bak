<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CreaWeb - Landing Pages en 24 Horas</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;500;700;900&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #001f3f 0%, #000 100%);
            color: #fff;
            line-height: 1.6;
            overflow-x: hidden;
        }
        html { scroll-behavior: smooth; }

        header {
            padding: 20px 0;
            position: fixed;
            width: 100%;
            top: 0;
            background: rgba(0, 0, 0, 0.4);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(10px);
            z-index: 1000;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.37);
            transition: padding 0.3s ease, background-color 0.3s ease;
        }

        nav {
            display: flex;
            align-items: center;
        }

        .logo {
            font-size: 24px;
            font-weight: bold;
            color: #D4AF37;
            display: flex;
            align-items: center;
            font-family: 'Orbitron', sans-serif;
            cursor: pointer;
            transition: text-shadow 0.4s ease;
        }

        .logo i {
            margin-right: 10px;
            color: #D4AF37;
            transition: transform 0.6s cubic-bezier(0.68, -0.55, 0.27, 1.55);
        }

        .logo:hover {
            text-shadow: 0 0 15px rgba(212, 175, 55, 0.7);
        }

        .logo:hover i {
            transform: rotate(360deg) scale(1.2);
        }

        .nav-links {
            display: flex;
            margin-left: auto; /* Empuja el menú a la derecha */
            padding-right: 20px; /* Añade espacio a la derecha del menú */
            gap: 30px;
        }

        .nav-links a {
            color: #fff;
            text-decoration: none;
            font-weight: 500;
            transition: color 0.3s;
            position: relative;
            font-family: 'Orbitron', sans-serif;
        }

        .nav-links a:hover {
            color: #D4AF37;
        }

        .nav-links a::after {
            content: '';
            position: absolute;
            bottom: -5px;
            left: 0;
            width: 0;
            height: 2px;
            background: #D4AF37;
            transition: width 0.3s;
        }

        .nav-links a:hover::after {
            width: 100%;
        }

        .nav-btn {
            background: rgba(212, 175, 55, 0.9);
            color: #000;
            padding: 12px 30px;
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 30px;
            font-weight: bold;
            cursor: pointer;
            transition: all 0.4s ease;
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            position: relative;
            overflow: hidden;
            text-decoration: none;
            font-family: 'Orbitron', sans-serif;
        }

        .nav-btn::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;
            height: 0;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.6);
            transform: translate(-50%, -50%);
            transition: width 0.6s, height 0.6s;
        }

        .nav-btn:hover::before {
            width: 300px;
            height: 300px;
        }

        .nav-btn:hover {
            background: rgba(255, 255, 255, 0.95);
            transform: translateY(-2px);
            box-shadow: 0 0 30px rgba(255, 255, 255, 0.6), 0 10px 30px rgba(212, 175, 55, 0.3);
            border-color: rgba(255, 255, 255, 0.4);
        }

        /* Estilos para enlaces legales en el footer */
        .copyright .legal-links {
            margin-top: 8px;
        }

        .copyright .legal-links a {
            color: #cfc39a;
            text-decoration: none;
            font-size: 0.95rem;
            transition: color 0.2s ease;
        }

        .copyright .legal-links a:hover {
            color: #fff;
            text-decoration: underline;
        }

        /* Modal legal */
        .legal-modal { 
            position: fixed; 
            inset: 0; 
            display: none; 
            align-items: center; 
            justify-content: center; 
            z-index: 2000; 
            padding: 20px; 
        }

        .legal-modal.open { display: flex; }

        .legal-modal-backdrop {
            position: absolute; inset: 0; background: rgba(0,0,0,0.7); backdrop-filter: blur(4px);
        }

        .legal-modal-content {
            position: relative; width: 90%; max-width: 1000px; height: 80%; background: #0b1220; border-radius: 10px; overflow: hidden; box-shadow: 0 20px 60px rgba(0,0,0,0.6);
        }

        .legal-modal-close {
            position: absolute; top: 10px; right: 12px; z-index: 5; background: transparent; border: 0; color: #fff; font-size: 30px; cursor: pointer; padding: 6px 10px; opacity: 0.95;
        }

        @media (max-width: 600px) {
            .legal-modal-content { height: 85%; }
        }

        /* Estilos específicos para contenido legal */
        .legal-modal-body h2 {
            margin-top: 0; color: #ffd; font-size: 1.6rem; margin-bottom: 12px; font-family: 'Orbitron', sans-serif;
        }

        .legal-modal-body h3 {
            color: #e6d9b8; margin-top: 14px; margin-bottom: 6px; font-size: 1.02rem;
        }

        .legal-modal-body p { color: #d8d2b2; line-height: 1.6; margin-bottom: 10px; }

        .legal-consent {
            display: flex; align-items: center; gap: 12px; margin-top: 18px; flex-wrap: wrap; justify-content: flex-end;
            border-top: 1px solid rgba(255,255,255,0.04); padding-top: 14px;
        }

        .legal-consent label { color: #d8d2b2; font-size: 0.95rem; }

        .legal-consent input[type="checkbox"] { width: 18px; height: 18px; }

        .legal-btn {
            background: #D4AF37; color: #000; padding: 10px 18px; border-radius: 8px; border: none; cursor: pointer; font-weight: 700; font-family: 'Orbitron', sans-serif;
            transition: opacity 0.2s ease, transform 0.08s ease; opacity: 0.95;
        }

        .legal-btn[disabled] { opacity: 0.45; cursor: not-allowed; }

        /* Toast de confirmación */
        .legal-toast {
            position: fixed;
            left: 50%;
            transform: translateX(-50%) translateY(24px);
            bottom: 24px;
            background: rgba(212,175,55,0.96);
            color: #000;
            padding: 12px 18px;
            border-radius: 8px;
            box-shadow: 0 6px 20px rgba(0,0,0,0.4);
            font-weight: 700;
            font-family: 'Orbitron', sans-serif;
            opacity: 0;
            pointer-events: none;
            transition: transform 260ms cubic-bezier(.2,.9,.3,1), opacity 260ms ease;
            z-index: 3000;
        }

        .legal-toast.show {
            opacity: 1; transform: translateX(-50%) translateY(0); pointer-events: auto;
        }



        /* Hero Section */
... (file backup continues)
